name: movil
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # Dependencias del proyecto
  http: ^1.1.0
  shared_preferences: ^2.2.2
  image: ^4.0.0             # NUEVO - Para comprimir imágenes
  path_provider: ^2.1.0     # Ya existe
  connectivity_plus: ^5.0.2 # NUEVO - Verificar internet
  geolocator: ^10.1.0
  image_picker: ^1.0.4
  signature: ^5.4.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true