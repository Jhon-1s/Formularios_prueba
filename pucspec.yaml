name: form_builder_movil
description: App móvil para formularios dinámicos
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.0.5
  intl: ^0.18.1  - para formato de fechas 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  